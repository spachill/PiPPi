<h2 class="custom__title">{{ title }}</h2>
<span class="custom__subtitle">{{ subtitle }}</span>
<style>
      .custom__title {
            text-align: center;
    margin: 0;
    font-size: 48px;
    font-weight: 500;
    letter-spacing: 0em;
    color: #baab9b;
    font-family: Noto Sans JP;
    line-height: calc(34 / 24);

      }

      .custom__subtitle {
            text-align: center;
    display: block;
    font-size: 16px;
    font-family: Noto Sans JP;
    margin-top: 8px;
    line-height: calc(29 / 24);
    color: #baab9b;

      }

      @media screen and (max-width: 768px) {
            .custom__title {
                  font-size: 30px;
                  line-height: 1.2;
            }
      }
</style>